# OpenClaw Adapter

这里定义如何把 `agents/<name>/` 的宿主无关配置映射到 OpenClaw 风格的 agent 目录。

## 映射建议

- `AGENTS.md` -> 行为规则文件
- `SOUL.md` -> 人格文件
- `PROFILE.yaml` -> 结构化元数据
- `SKILLS.yaml` -> 技能绑定
- `TOOLS.yaml` -> 工具策略

## 当前状态

第一版只建立结构预留，不直接生成 OpenClaw 兼容文件。
